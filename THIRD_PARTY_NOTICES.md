# Third-party notices

## gflow-cli

The Edge/Flow automation strategy and resilient selector approach were informed by
[`ffroliva/gflow-cli`](https://github.com/ffroliva/gflow-cli), pinned to tag `v0.49.0`
and commit `127c3cc873ca777d5744b0e94dc3dec22337efe9`.

`gflow-cli` is MIT licensed. Its source is not copied wholesale into this repository.
Run `scripts/bootstrap_gflow.ps1` to obtain the exact audited snapshot in
`vendor/gflow-cli`.

## Autoflow Chrome extension

The web interface uses a new, original implementation of common batch-automation UI
patterns (side navigation, prompt queue, progress, logs, import and download controls).
No code, icons, screenshots, branding or packaged assets were copied from the closed
Chrome Web Store extension `eklokclnmljbdijdhnamfcdhfomdmbmg`.
