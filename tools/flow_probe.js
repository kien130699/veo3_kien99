/* Dán vào Console tab Flow để chẩn đoán selector, không tự nhập hoặc submit. */
(()=>{const selectors=['[data-slate-editor="true"][contenteditable="true"]','[data-slate-editor="true"]','.ProseMirror[contenteditable="true"]','[role="textbox"][contenteditable="true"]','textarea[placeholder*="prompt" i]'];const visible=e=>{const r=e.getBoundingClientRect(),s=getComputedStyle(e);return r.width>2&&r.height>2&&s.display!=='none'&&s.visibility!=='hidden'};const editors=selectors.flatMap(selector=>[...document.querySelectorAll(selector)].filter(visible).map(element=>({selector,element})));console.table(editors.map((item,index)=>({index,selector:item.selector,text:(item.element.innerText||item.element.textContent||'').slice(0,120),role:item.element.getAttribute('role')})));editors[0]?.element.scrollIntoView({block:'center'});if(editors[0])editors[0].element.style.outline='4px solid #9b6cff';return{url:location.href,editors:editors.length}})();
